package com.sesasis.donusum.yok.repository;

import com.sesasis.donusum.yok.entity.AnaDomainSlider;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AnaDomainSliderRepository extends JpaRepository<AnaDomainSlider, Long> {
}