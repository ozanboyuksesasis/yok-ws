package com.sesasis.donusum.yok.repository;

import com.sesasis.donusum.yok.entity.DuyuruDilCategory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DuyuruDilCategoryRepository extends JpaRepository<DuyuruDilCategory,Long> {
}
